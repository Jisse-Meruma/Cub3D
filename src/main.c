#include "libft.h"
#include "MLX42.h"

int main(int argc, char *argv[])
{
	if (argc != 2)
	{
		printf("Error invalid arguments: Usage: ./cub3d + ANOTHER ARGUMENT stoopid..");
		return (EXIT_FAILURE);
	}
	
}